/// 앱의 가상 위치 프리셋(부산/속초/강릉 등)과 실제 두루누비 코스를 미리
/// 연결해 두는 매핑 테이블.
///
/// 두루누비 courseList API는 좌표/반경 기반 검색과 코스ID 직접 조회를
/// 모두 지원하지 않는다 — 요청 파라미터는 crsKorNm(코스명), routeIdx(길
/// 고유번호), crsLevel(난이도), brdDiv(걷기/자전거) 뿐이다 (한국관광공사_
/// TourAPI활용매뉴얼(두루누비)_v4.1 III-3-나 참고). "내 위치 근처 코스"를
/// 보여주려면 매 요청마다 전국 코스를 순회하며 GPX를 받아 좌표를 확인해야
/// 하는데, 이는 개발계정 일일 트래픽(1,000건)을 순식간에 소진시킨다.
///
/// 그래서 이 앱은 런타임 검색 대신, 가상 위치 프리셋마다 실제로 그 지역에
/// 있는 두루누비 코스를 사람이 미리 조사해 "코스명"으로 고정해 둔다.
/// 앱은 이 코스명으로 courseList(crsKorNm 검색) + GPX를 가져오므로
/// 호출 횟수가 항상 예측 가능하고 작다.
///
/// 코스명 검색이 완전 일치가 아니라 부분 일치일 가능성이 있어(API 응답으로
/// 실제 검증 필요), 값은 반드시 실제 courseList 호출로 돌아온 crsKorNm
/// 원문 그대로 넣어야 한다 — 사람이 임의로 줄이거나 바꾸면 검색이 0건이
/// 될 수 있다.
///
/// 아래 목록은 DURUNUBI_API_KEY로 courseList(brdDiv=DNWW) 전체(142건)를
/// 실제로 조회해, 각 가상 위치 좌표와 코스 GPX 경로점 사이의 하버사인
/// 거리를 계산해 가장 가까운 코스를 선정한 결과다 (2026-09-10 검증).
/// 각 코스의 gpxpath도 실제로 다운로드해 유효한 GPX(trk/trkseg/trkpt)임을
/// 확인했다.
///
///   부산(areaCode 6, 부산광역시청 35.1796,129.0756 기준)
///     - 해파랑길 2코스 (부산 해운대구, 시작점까지 약 8.0km)
///     - 해파랑길 1코스 (부산 남구, 중간점까지 약 7.0km)
///   강원(areaCode 32, 속초/강릉 프리셋 공통 — 두 프리셋 모두 이 지역코드로
///   매핑되어 있어 강원권 코스를 함께 담는다)
///     - 해파랑길 45코스 (강원 속초시, 속초 프리셋 기준 약 3.1km)
///     - 해파랑길 39코스 바우길 05구간 (강원 강릉시, 강릉 프리셋 기준 약 2.2km)
///     - 해파랑길 40코스 바우길 12구간 (강원 강릉시, 강릉 프리셋 기준 중간점 약 5.7km)
///
/// 새 가상 위치 프리셋을 추가할 때는 같은 방식(courseList 전체 조회 →
/// GPX 다운로드 → 거리 계산)으로 재조사해서 채워 넣는다.
const Map<String, List<String>> durunubiCourseNamesByAreaCode = {
  '6': ['해파랑길 2코스', '해파랑길 1코스'], // 부산
  '32': [
    '해파랑길 45코스',
    '해파랑길 39코스 바우길 05구간',
    '해파랑길 40코스 바우길 12구간',
  ], // 강원(속초/강릉)
};
